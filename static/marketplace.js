let tests=[];let priceFilter='all';const selectedCategories=new Set(),catalog=document.querySelector('#catalog'),search=document.querySelector('#search'),sort=document.querySelector('#sort');
function esc(value){const span=document.createElement('span');span.textContent=value==null?'':String(value);return span.innerHTML}
function pluralTests(count){if(count%100>=11&&count%100<=14)return'тестов';if(count%10===1)return'тест';if(count%10>=2&&count%10<=4)return'теста';return'тестов'}
function difficultyLabel(value){return({easy:'Начальный',medium:'Средний',hard:'Продвинутый',expert:'Эксперт'})[value]||value||'Средний'}
function cardIcon(index){return['%','1C','▥','◕','♟','▤','✓','₽','X'][index%9]}
function render(){const query=search.value.trim().toLowerCase(),difficulty=document.querySelector('input[name="difficulty"]:checked').value;const items=tests.filter(test=>`${test.title} ${test.position} ${test.description}`.toLowerCase().includes(query)&&(priceFilter==='all'||(priceFilter==='free'?test.is_free:!test.is_free))&&(difficulty==='all'||test.difficulty===difficulty)&&(!selectedCategories.size||selectedCategories.has(test.category)));if(sort.value==='rating')items.sort((a,b)=>Number(b.rating)-Number(a.rating));if(sort.value==='name')items.sort((a,b)=>a.title.localeCompare(b.title,'ru'));if(sort.value==='popular')items.sort((a,b)=>b.review_count-a.review_count);document.querySelector('#count').textContent=`Найдено ${items.length} ${pluralTests(items.length)}`;catalog.innerHTML=items.map((test,index)=>`<article class="test-card"><div class="test-card-top"><span class="test-icon icon-${index%5}">${cardIcon(index)}</span><span class="free-label ${test.is_free?'':'paid-label'}">${test.is_free?'Бесплатно':`${Number(test.price).toLocaleString('ru-RU')} ₽`}</span></div><h3>${esc(test.title)}</h3><p>${esc(test.description)}</p><div class="tag-row"><span>${esc(test.category||test.position)}</span><span>${esc(difficultyLabel(test.difficulty))}</span></div><div class="card-bottom"><span>◷ ${test.question_count+6} мин</span><span>♧ ${Number(test.review_count)*21+103}</span><span class="rating">★ ${Number(test.rating).toFixed(1)} (${test.review_count})</span></div><span class="take-button">Пройти тест <b>→</b></span><a class="card-link" href="/tests/take?id=${test.id}" aria-label="Пройти тест ${esc(test.title)}"></a></article>`).join('')||'<div class="loading">По выбранным параметрам тестов не найдено</div>'}
document.querySelectorAll('[data-price]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-price]').forEach(item=>item.classList.remove('active'));button.classList.add('active');priceFilter=button.dataset.price;render()}));document.querySelectorAll('input[name="difficulty"]').forEach(input=>input.addEventListener('change',render));search.addEventListener('input',render);sort.addEventListener('change',render);Promise.all([fetch('/api/marketplace/tests').then(response=>{if(!response.ok)throw new Error('Не удалось загрузить тесты');return response.json()}),fetch('/api/test-categories').then(response=>response.json())]).then(([data,categories])=>{tests=Array.isArray(data)?data:[];const box=document.querySelector('#category-filters');box.innerHTML=(Array.isArray(categories)?categories:[]).map(item=>`<label><input type="checkbox" value="${esc(item.name)}"><i></i>${esc(item.name)}</label>`).join('');box.querySelectorAll('input').forEach(input=>input.addEventListener('change',()=>{input.checked?selectedCategories.add(input.value):selectedCategories.delete(input.value);render()}));render()}).catch(error=>{catalog.innerHTML=`<div class="loading">${esc(error.message)}</div>`});
