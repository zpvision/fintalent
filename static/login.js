const form=document.querySelector('#login-form'),message=document.querySelector('#form-message'),password=form.elements.password;
document.querySelector('.show-password').addEventListener('click',e=>{password.type=password.type==='password'?'text':'password';e.currentTarget.textContent=password.type==='password'?'◉':'⊘'});
form.addEventListener('submit',async e=>{e.preventDefault();message.className='form-message';if(!form.reportValidity())return;const button=form.querySelector('[type="submit"]');button.disabled=true;button.textContent='Входим…';try{const response=await fetch(form.action,{method:'POST',body:new FormData(form)}),result=await response.json();if(!response.ok)throw new Error(result.error||'Не удалось войти');window.location.href='/'}catch(error){message.className='form-message error';message.textContent=error.message;button.disabled=false;button.innerHTML='Войти <span>→</span>'}});
